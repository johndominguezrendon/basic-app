import React from 'react';

const Hero = () => {
    return (
        <div className="hero">
            <h1 className="title">Find Your Train</h1>
            <h2 className="subtitle">Departure and Arrival times so you don't miss 'em!</h2>
        </div>
    );
};

export default Hero;